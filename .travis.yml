language: java

jdk:
  - oraclejdk8

branches:
  only:
    - master

cache:
  directories:
    - $HOME/.gradle

before_script:
  - chmod +x gradlew

script:
  - ./gradlew setupDecompWorkspace idea
  - ./gradlew clean build
